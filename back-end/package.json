{
  "name": "alignment-backend",
  "version": "1.0.0",
  "description": "Backend for alignment project using Zod schemas",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "npx vitest run",
    "test:watch": "npx vitest",
    "test:coverage": "npx vitest run --coverage"
  },
  "dependencies": {
    "@zodyac/zod-mongoose": "*",
    "express": "^4.18.2",
    "mongoose": "^7.5.0",
    "supertest": "^7.1.0",
    "zod": "*"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "superagent": "^8.0.0",
    "vitest": "^0.34.0"
  }
}
